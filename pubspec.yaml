# ================================================
# DangunDad Flutter App - pubspec.yaml Template
# ================================================
# ?ъ슜踰? ???꾨줈?앺듃?????댁슜??蹂듭궗 ??word_guess_game, Guess the 5-letter word in 6 tries! 移섑솚

name: word_guess_game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  get: ^4.7.3
  # 濡쒖뺄 ??μ냼
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  path_provider: ^2.1.5

  # UI
  flutter_screenutil: ^5.9.3
  flex_color_scheme: ^8.4.0
  google_fonts: ^6.3.2
  lucide_icons_flutter: ^3.1.9

  # 愿묎퀬
  google_mobile_ads: ^6.0.0
  in_app_purchase: ^3.2.3

  # ?좏떥由ы떚
  intl: ^0.20.2
  share_plus: ^12.0.1

  # ---- ?깅퀎 異붽? ?⑦궎吏 (?꾩슂???꾨옒 二쇱꽍 ?댁젣) ----
  # fl_chart: ^0.70.2              # 李⑦듃 (媛怨꾨?, ?듦?, ?듦퀎)
  # flutter_local_notifications:    # ?뚮┝ (??대㉧, 由щ쭏?몃뜑)
  # just_audio: ^0.9.42            # ?ㅻ뵒???ъ깮 (?섎㈃ ?ъ슫??
  # mobile_scanner: ^6.0.5         # QR/諛붿퐫???ㅼ틪
  # qr_flutter: ^4.1.0             # QR 肄붾뱶 ?앹꽦
  # http: ^1.3.0                   # HTTP ?붿껌 (?섏쑉 API, ?댁쫰 API)
  # pedometer: ^4.0.2              # 留뚮낫湲??쇱꽌

  device_info_plus: ^12.3.0
  firebase_analytics: ^12.1.2
  firebase_core: ^4.4.0
  firebase_crashlytics: ^5.0.7
  flutter_animate: ^4.5.2
  gma_mediation_applovin: ^2.5.1
  gma_mediation_pangle: ^3.5.0
  gma_mediation_unity: ^1.6.2
  in_app_review: ^2.0.11
  package_info_plus: ^9.0.0
  permission_handler: ^12.0.1
  rate_my_app: ^2.3.2
  url_launcher: ^6.3.2
  uuid: ^4.5.3
  vibration: ^3.1.7
  wakelock_plus: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.11.1
  build_runner: ^2.11.0
  change_app_package_name: ^1.5.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/
    - assets/images/
    - assets/data/
    # - assets/sounds/             # ?ъ슫???깆슜
  fonts:
    - family: NotoSans
      fonts:
        - asset: assets/fonts/NotoSans-Bold.ttf
          weight: 700
